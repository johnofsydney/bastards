<p style="color: green"><%= notice %></p>

<h1>All Candidates by party</h1>
<%= pie_chart Candidate.group(:party).count.map{ |k, v| [k.name, v] }.to_h, colors: ['red', 'blue', 'green', 'brown', 'orange', 'yellow', 'teal'] %>
https://github.com/ankane/chartkick/issues/220

<h1>All Candidates by Religion</h1>
<%= column_chart Candidate.joins(:religion).group(:religion).count.map{ |k, v| [k.name, v] }.to_h %>

<h1>All Candidates by Gender</h1>
<%= column_chart Candidate.group(:gender).count %>

<h1>All Candidates by Previous Career</h1>
<h3>Complicated! coming soon...</h3>



==============
<h1>Candidates</h1>

<div id="candidates">
  <% @candidates.each do |candidate| %>
    <%= render candidate %>
    <p>
      <%= link_to "Show this candidate", candidate %>
    </p>
  <% end %>
</div>

<%= link_to "New candidate", new_candidate_path %>


https://chartkick.com/